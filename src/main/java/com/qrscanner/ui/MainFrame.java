import javax.swing.*;\nimport java.awt.*;\nimport java.awt.event.*;\n\npublic class MainFrame extends JFrame {\n    private JPanel cameraPanel;\n    private JPanel resultsPanel;\n    private JPanel controlPanel;\n    private JLabel statusBar;\n\n    public MainFrame() {\n        setTitle("QR Scanner");\n        setSize(800, 600);\n        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);\n        setLocationRelativeTo(null);\n        setLayout(new BorderLayout());\n\n        // Create panels\n        createPanels();\n        // Add components\n        addComponents();\n        // Set gradient background\n        setGradientBackground();\n    }\n\n    private void createPanels() {\n        cameraPanel = new JPanel();\n        resultsPanel = new JPanel();\n        controlPanel = new JPanel();\n        statusBar = new JLabel("Status: Ready");\n    }\n\n    private void addComponents() {\n        add(cameraPanel, BorderLayout.CENTER);\n        add(resultsPanel, BorderLayout.SOUTH);\n        add(controlPanel, BorderLayout.NORTH);\n        add(statusBar, BorderLayout.PAGE_END);\n\n        // Add control buttons\n        JButton startButton = new JButton("Start");\n        JButton stopButton = new JButton("Stop");\n        controlPanel.add(startButton);\n        controlPanel.add(stopButton);\n\n        // Add action listeners\n        startButton.addActionListener(e -> startScanning());\n        stopButton.addActionListener(e -> stopScanning());\n    }\n\n    private void setGradientBackground() {\n        cameraPanel.setBackground(new Color(0, 128, 255)); // Example gradient color\n        resultsPanel.setBackground(new Color(255, 255, 255)); // White background for results\n        controlPanel.setBackground(new Color(0, 102, 204)); // Control panel color\n    }\n\n    private void startScanning() {\n        statusBar.setText("Status: Scanning...");\n        // Add code to initiate camera scanning\n    }\n\n    private void stopScanning() {\n        statusBar.setText("Status: Stopped");\n        // Add code to stop camera scanning\n    }\n\n    public static void main(String[] args) {\n        SwingUtilities.invokeLater(() -> {\n            MainFrame frame = new MainFrame();\n            frame.setVisible(true);\n        });\n    }\n}